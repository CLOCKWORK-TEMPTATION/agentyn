# نظام الوكلاء المتقدم (Advanced Multi-Agent System)

نظام ذكي متعدد الوكلاء مبني باستخدام TypeScript و LangChain مع دعم Google ADK وأكثر من 15 أداة متخصصة.

## 🌟 الميزات الرئيسية

### 🤖 أنواع الوكلاء

1. **الوكيل المدمج (Hybrid Agent)**
   - دمج LangChain + RAG
   - 15 أداة متخصصة
   - دعم OpenAI و Anthropic

2. **نظام الوكلاء المتعددة (Multi-Agent System)**
   - 🔍 **باحث** - البحث والمعلومات
   - 💻 **مطور** - البرمجة والكود
   - ✍️ **كاتب** - المحتوى والمقالات
   - 📊 **محلل** - الحسابات والبيانات
   - ⚙️ **مدير نظام** - الملفات والأوامر

3. **Google ADK Integration**
   - دعم Gemini Models
   - ذاكرة طويلة المدى
   - أدوات Google المتقدمة

### 🛠️ الأدوات المتاحة (15 أداة)

- **📁 إدارة الملفات**: قراءة، كتابة، تعديل
- **💻 تنفيذ الأوامر**: Bash/PowerShell
- **🔍 البحث**: Glob patterns، Grep في المحتوى
- **🌐 الويب**: جلب المحتوى، بحث محاكي
- **📋 المهام**: إنشاء قوائم TODO
- **🎯 المهارات**: تحليل الكود، إنشاء الوثائق
- **⚡ الأوامر السريعة**: /help، /status، /clean

### 🚀 أنماط التنفيذ

1. **ذكي (Smart)** - اختيار الوكيل الأنسب تلقائياً
2. **متوازي (Parallel)** - عدة وكلاء في نفس الوقت
3. **تسلسلي (Sequential)** - وكلاء يبنون على نتائج بعض

## 📦 التثبيت والإعداد

### المتطلبات

- Node.js 18+
- npm أو yarn
- مفاتيح API (اختياري):
  - `OPENAI_API_KEY`
  - `ANTHROPIC_API_KEY`
  - `GOOGLE_API_KEY`

### التثبيت

```bash
# استنساخ المشروع
git clone <repository-url>
cd agents

# تثبيت التبعيات
npm install

# إعداد متغيرات البيئة
cp .env.example .env
# عدّل ملف .env وأضف مفاتيح API
```

### إعداد متغيرات البيئة

```bash
# ملف .env
OPENAI_API_KEY=your_openai_key_here
ANTHROPIC_API_KEY=your_anthropic_key_here
GOOGLE_API_KEY=your_google_key_here
```

## 🚀 التشغيل

### تشغيل خادم API

```bash
# تشغيل خادم API مع واجهة الويب
npm run server

# أو للتطوير
npm run dev
```

الخادم سيعمل على: `http://localhost:3000`

### تشغيل الوكلاء منفصلين

```bash
# الوكيل المدمج
npm run hybrid

# وكيل RAG
npm run rag

# نظام الوكلاء المتعددة
npm run multi-agent

# Google ADK Integration
npm run google-adk
```

## 🌐 واجهة الويب

افتح `http://localhost:3000` في المتصفح للوصول لواجهة الويب التفاعلية.

### الميزات:
- اختيار نوع الوكيل
- أمثلة سريعة
- عرض الإحصائيات
- واجهة عربية جميلة

## 📡 API Endpoints

### الوكيل المدمج
```bash
POST /api/agent/query
{
  "query": "سؤالك هنا"
}
```

### وكيل RAG
```bash
POST /api/rag/query
{
  "query": "سؤال عن المعرفة"
}
```

### نظام الوكلاء المتعددة

#### ذكي (اختيار تلقائي)
```bash
POST /api/multi-agent/query
{
  "query": "سؤالك هنا",
  "sessionId": "session_123" // اختياري
}
```

#### تنفيذ متوازي
```bash
POST /api/multi-agent/parallel
{
  "query": "سؤالك هنا",
  "agents": ["researcher", "developer"],
  "sessionId": "session_123" // اختياري
}
```

#### تنفيذ تسلسلي
```bash
POST /api/multi-agent/sequential
{
  "query": "سؤالك هنا",
  "agents": ["researcher", "writer", "analyst"],
  "sessionId": "session_123" // اختياري
}
```

### إحصائيات النظام
```bash
GET /api/stats
GET /api/health
```

## 🧪 أمثلة الاستخدام

### أمثلة بسيطة

```bash
# بحث ومعلومات
"ابحث عن آخر أخبار الذكاء الاصطناعي"

# برمجة
"اكتب كود Python لحساب الأعداد الأولية"

# حسابات
"احسب متوسط الأرقام: 15, 23, 31, 47"

# كتابة
"أنشئ مقال عن الطاقة المتجددة"

# إدارة ملفات
"اقرأ ملف package.json وحلل التبعيات"
```

### أمثلة متقدمة (Multi-Agent)

```bash
# بحث + تطوير (متوازي)
"ابحث عن React وأنشئ مثال بسيط"
# الوكلاء: ["researcher", "developer"]

# بحث → كتابة → مراجعة (تسلسلي)
"ابحث عن موضوع مثير، اكتب مقال، ثم راجعه"
# الوكلاء: ["researcher", "writer", "analyst"]
```

## 🏗️ البنية التقنية

```
src/
├── hybrid-agent.ts          # الوكيل المدمج الرئيسي
├── multi-agent-system.ts    # نظام الوكلاء المتعددة
├── google-adk-integration.ts # تكامل Google ADK
├── advanced-tools.ts        # 15 أداة متخصصة
├── rag-agent.ts            # وكيل RAG
├── api-server.ts           # خادم API
└── test-advanced-agent.ts  # اختبارات

public/
└── index.html              # واجهة الويب

documents/                  # قاعدة المعرفة
├── ai-guide.md
├── programming-guide.md
└── ...
```

## 🔧 التخصيص والتطوير

### إضافة وكيل جديد

```typescript
// في multi-agent-system.ts
const customAgent = new HybridAgent('openai');
await customAgent.initialize();

multiAgentSystem.addAgent(
  'custom',
  'وكيل مخصص',
  customAgent,
  ['تخصص1', 'تخصص2'],
  6 // الأولوية
);
```

### إضافة أداة جديدة

```typescript
// في advanced-tools.ts
export const customTool = new DynamicTool({
  name: "custom_tool",
  description: "وصف الأداة",
  func: async (input: string) => {
    // منطق الأداة
    return "النتيجة";
  },
});
```

## 📊 الإحصائيات والمراقبة

- **الذاكرة المستخدمة**: مراقبة استهلاك الذاكرة
- **وقت التشغيل**: مدة عمل النظام
- **إحصائيات الوكلاء**: حالة كل وكيل
- **أداء الأدوات**: أوقات التنفيذ

## 🐛 استكشاف الأخطاء

### مشاكل شائعة

1. **مفاتيح API مفقودة**
   ```bash
   # تحقق من ملف .env
   cat .env
   ```

2. **خطأ في التبعيات**
   ```bash
   # إعادة تثبيت
   rm -rf node_modules package-lock.json
   npm install
   ```

3. **خطأ في TypeScript**
   ```bash
   # فحص الأخطاء
   npx tsc --noEmit
   ```

## 🤝 المساهمة

1. Fork المشروع
2. إنشاء branch جديد (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'Add amazing feature'`)
4. Push للـ branch (`git push origin feature/amazing-feature`)
5. فتح Pull Request

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT - راجع ملف [LICENSE](LICENSE) للتفاصيل.

## 🙏 شكر وتقدير

- [LangChain](https://langchain.com/) - إطار عمل الوكلاء
- [OpenAI](https://openai.com/) - نماذج GPT
- [Anthropic](https://anthropic.com/) - نماذج Claude
- [Google](https://ai.google.dev/) - Gemini و ADK
- [LlamaIndex](https://llamaindex.ai/) - RAG وإدارة المستندات

---

**نجاح موفق في استخدام نظام الوكلاء المتقدم! 🚀**